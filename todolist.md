# Todolist

## 已完成

- [x] 1. 锁屏息屏后，闹钟提醒时，没有亮屏，亮屏的话，闹钟提醒如何显示在锁屏层级之上？

- [x] 2. 倒计时暂停，等一会儿再继续，倒计时到时间后，只有应用内的弹窗提醒，没有系统的闹钟提醒，我猜想应该是暂停也要对系统的计时器发出暂停系统计时器通知，继续也要发出恢复系统计时器通知吧。

- [x] 3. 系统闹钟目前只有铃声，需要把持续振动也加上

- [x] 4. Android 原生闹钟链路接入：使用 `AlarmManager.setAlarmClock` 调度阶段提醒，并通过原生广播接收器处理到期事件。

- [x] 5. 后台/其他应用场景接入悬浮窗提醒：到点后通过 `AlarmOverlayService` 展示覆盖提醒，并保留通知栏兜底入口。

- [x] 6. 锁屏/息屏场景接入原生闹钟页面：`AlarmAlertActivity` 负责锁屏层级展示、亮屏、响铃和振动。

- [x] 7. 前台到点时优先请求原生闹钟呈现，失败后再退回应用内弹窗提醒。

- [x] 8. 权限引导改成渐进式场景说明：步骤 1 后台提醒对应悬浮窗，步骤 2 锁屏提醒对应后台弹出界面和锁屏显示。

- [x] 9. 首页保留常驻轻量权限入口：权限小图标、一行简短提示文案、开启按钮。

- [x] 10. 去掉后台弹出界面、锁屏显示的手动勾选确认，避免用户没有真正打开权限却手动标记完成。

- [x] 11. 当悬浮窗已开启时，权限弹窗不展示步骤 1；锁屏提醒仍展示，但标题去掉“步骤 2”。

- [x] 12. Android 返回手势优化：弹窗优先关闭，子页面返回主页，主页连续两次返回才退出。

- [x] 13. 日志页增加分页、加载更多、多选复制，方便真机调试和反馈。

- [x] 14. 将 Android 原生日志同步到 App 日志页，便于排查调度、广播、Activity、Overlay Service、通知、响铃和振动链路。

## 待真机验证

- [ ] 1. 应用在前台时，阶段到点是否优先弹出原生闹钟提醒；如果失败，是否退回应用内弹窗。

- [ ] 2. 应用切到后台后，阶段到点是否能收到通知、响铃、振动，并展示浮窗提醒。

- [ ] 3. 用户正在浏览其他应用时，悬浮窗权限开启后是否能覆盖当前应用显示提醒。

- [ ] 4. 锁屏/息屏场景下，后台弹出界面和锁屏显示开启后，提醒是否能亮屏并显示在锁屏层级。

- [ ] 5. 未开启悬浮窗时，到点提醒是否至少保留通知栏兜底入口。

- [ ] 6. 从系统设置页返回 App 后，悬浮窗权限是否会自动重新检测，且已开启时权限弹窗能正确关闭。

- [ ] 7. 当悬浮窗已开启但锁屏权限未处理时，首页常驻权限入口是否仍然存在，点击后是否还能进入权限弹窗。

- [ ] 8. 权限弹窗在“有步骤 1”和“无步骤 1”两种状态下，文案和卡片标题是否符合预期。

- [ ] 9. Android 返回手势是否符合顺序：关闭弹窗、日志页回主页、主页第一次提示、第二次退出。

- [ ] 10. 日志页加载更多、多选复制、清空日志后状态重置是否正常。

## 后续可优化

- [ ] 1. 根据更多真机反馈，补充 MIUI、ColorOS、OriginOS、HarmonyOS 等系统的权限页跳转路径和文案。

- [ ] 2. 根据真实用户理解情况，继续压缩权限弹窗文案，减少阅读负担。

- [ ] 3. 如果后续引入正式导航栈，需要把当前 `getAndroidBackAction` 的返回优先级迁移到导航返回策略中。

- [ ] 4. 打包发布前移除或调整 `index.js` 里的测试版本浮层文案。
